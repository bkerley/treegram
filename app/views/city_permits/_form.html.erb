<%= form_with(model: city_permit, local: true) do |form| %>
  <% if city_permit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city_permit.errors.count, "error") %> prohibited this city_permit from being saved:</h2>

      <ul>
      <% city_permit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :plan_number %>
    <%= form.text_field :plan_number, id: :city_permit_plan_number %>
  </div>

  <div class="field">
    <%= form.label :property_address %>
    <%= form.text_field :property_address, id: :city_permit_property_address %>
  </div>

  <div class="field">
    <%= form.label :review_status %>
    <%= form.text_field :review_status, id: :city_permit_review_status %>
  </div>

  <div class="field">
    <%= form.label :review_status_changed_date %>
    <%= form.datetime_select :review_status_changed_date, id: :city_permit_review_status_changed_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
