<p id="notice"><%= notice %></p>

<h1>Permits</h1>

<table>
  <thead>
    <tr>
      <th>Notified at</th>
      <th>Address</th>
      <th>Location</th>
      <th>City permit</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @permits.each do |permit| %>
      <tr>
        <td><%= permit.notified_at %></td>
        <td><%= permit.address %></td>
        <td><%= permit.location %></td>
        <td><%= permit.city_permit %></td>
        <td><%= link_to 'Show', permit %></td>
        <td><%= link_to 'Edit', edit_permit_path(permit) %></td>
        <td><%= link_to 'Destroy', permit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Permit', new_permit_path %>
